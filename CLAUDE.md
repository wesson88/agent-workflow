# 项目工作规则

## 问题复盘强制沉淀

每次遇到问题，事后都需要把**问题原因**和**解决方案**写到项目记录中（vault 路径：`20-知识/项目记录/<问题主题>.md`）。

适用范围：所有项目工作期间，凡是触发以下任一情况的，都必须沉淀：
- 工作流跑挂、超时、子进程被 kill
- 角色产出错误（FILE 块缺失、路径漂移、内容偏离 spec 等）
- 治理代码改动（无论改动大小，原因 + 修法都要记）
- 用户提出的疑问需要分析后回答的（把分析意见连同结论记下）

沉淀文档结构最小要求：
1. **现象**：观测到的具体行为 + 复现路径
2. **根因**：定位到的代码/配置/prompt 层面的原因
3. **修法**：实际改动 + commit 引用
4. **风险/待办**：修复后仍可能复发的场景，或需要进一步实战验证的项

写完后在对话里告诉用户文档路径，便于他后续 Graph View / 搜索找到。

## 阈值来源必须显式声明

**硬约束**：所有出现在**规范 / 实施计划 / 测试判据 / role_auditor lint / 引擎护栏**的**数字阈值**都必须附一句依据。适用于百分比（如 60% 命中率）、字符数（如 5000 chars 软上限）、token 预算、超时秒数、次数（如连续失败 2 次）等一切数字。

### 依据的三种合法形式

| 类型 | 例子 |
|---|---|
| **数据溯源** | `# 依据：pain-radar 实测 5 项目综合平均 68%（P6 采样 2026-07-04）` |
| **推导逻辑** | `# 依据：业务场景平均涉及 30-50% skill，60% 是刻意宽松上限` |
| **明标"拍脑袋初值"** | `# 依据：初值，无数据支持；等 pain-radar 实战 1 轮后校准（挂 98-待办跟踪）` |

### 检查点

- 写规范文档时：每个阈值旁 1-2 行说明依据
- 写代码常量 / LIMITS dict / assert 阈值时：注释含依据
- role_auditor / 引擎护栏用的门槛：来源写在设置该门槛的模块注释里
- **无依据的阈值 → 在项目记录里补一条待验证项，或直接质疑作者**

### 反例（明确要避免的）

- ❌ 写 "≥ 60%" 或 `LIMITS["single_section"] = 1500` 不加任何注释
- ❌ 依据说 "经验值"、"合理值"、"业界标准"（等于没说，必须给具体证据）
- ❌ 依据用 "我觉得"、"感觉这样比较好"（不是依据）

### 触发条件

- 新增阈值 → 必须附依据
- 修改已有阈值 → 必须更新依据
- Review 别人代码/规范时看到无依据阈值 → 打回或补溯源
- 复盘时发现阈值实测背离（如本项目 60% → 68% 分域偏差巨大）→ 沉淀到 `20-知识/项目记录/`
