[pytest]
# 将项目根目录加入 sys.path，使 `src.backend.main` 可直接 import
pythonpath = .
# tests/        — vault 项目产出的代码测试（每个项目一份，被 .gitignore 排除）
# .claude/tests/ — 引擎本体自测（随引擎一起进 git）
testpaths = tests .claude/tests
